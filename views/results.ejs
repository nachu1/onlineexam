<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>All Results - Admin</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>All Submitted Results</h1>
    <table border="1" cellpadding="5">
        <thead>
            <tr>
                <th>Student Name</th>
                <th>Total Questions</th>
                <th>Score</th>
                <th>Correct</th>
                <th>Wrong</th>
                <th>Not Attempted</th>
                <th>Published</th>
            </tr>
        </thead>
        <tbody>
            <% results.forEach(r => { %>
                <tr>
                    <td><%= r.name %></td>
                    <td><%= r.totalQuestions %></td>
                    <td><%= r.score %></td>
                    <td><%= r.correct %></td>
                    <td><%= r.wrong %></td>
                    <td><%= r.notAttended %></td>
                    <td><%= r.published ? "Yes" : "No" %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>

    <form action="/admin/publish" method="POST">
        <button type="submit">Publish All Results</button>
    </form>
</body>
</html>
